<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- permission -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AntrianPraktekDokter"
        android:usesCleartextTraffic="true">

        <activity android:name=".DoctorPage.DokterActivity" />

        <activity
            android:name=".patient.NotifikasiActivity"
            android:exported="false" />
        <activity
            android:name=".patient.ListAntrianActivity"
            android:exported="false" />
        <activity
            android:name=".patient.ProfileActivity"
            android:exported="false" />
        <activity
            android:name=".patient.HistoryMedisActivity"
            android:exported="false" />
        <activity
            android:name=".patient.JanjiTemuActivity"
            android:exported="false" />
        <activity android:name=".patient.HomeActivity" />
        <activity android:name=".auth.LoginActivity" />
        <activity android:name=".auth.RegisterActivity" />
        <activity
            android:name=".auth.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>